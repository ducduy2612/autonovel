# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 |  | config | Language config mechanism | AUTONOVEL_LANGUAGE env var via .env | Consistent with existing model config pattern (AUTONOVEL_WRITER_MODEL, etc.). python-dotenv already loaded in all scripts. | No | collaborative |
| D002 |  | arch | Prompt adaptation strategy | Inline conditional instructions in each script | Minimal structural change. Each script already constructs its own prompts. Adding a conditional language instruction is the lightest approach for a single target language. | Yes — if adding more languages, refactor to shared prompt system | collaborative |
| D003 |  | quality | Vietnamese evaluation strategy | Skip English regex, keep language-agnostic stats, LLM judge evaluates Vietnamese directly | English regex (banned words, AI tells, show-don't-tell) cannot match Vietnamese and would false-pass. Language-agnostic stats (sentence variation, em-dash density) still provide value. LLM judge handles quality assessment natively in any language. | Yes — add Vietnamese-specific patterns when research is done | collaborative |
| D004 |  | convention | Character gender tracking per chapter for manga prompts | CH09: Bách(nam),Linh(nữ),Cô Bích,Linh(nữ); CH10: Bách(nam→still nam after Rửa),Linh(nữ); CH11: Linh(nữ),Lịch(nữ); CH12: Linh(nữ),Bách(nam),Lịch(nữ),Tuấn(nam),Duy(nữ); CH13: Bách(nam),Linh(nữ),Tuấn(nam),Lịch(nữ),Duy(nữ); CH14: Linh(nữ),Bà Lụa; CH15: Linh(nữ),Tuấn(nam),Lịch(nữ); CH16: Bách(nam),Cảnh(nam),Linh(nữ); CH17: Tuấn(nam,Rửa'd-loses memory of Lịch),Linh(nữ),Lịch(nữ); CH18: Linh(nữ),Lịch(nữ); CH19: Linh(nữ),Cảnh(nam); CH20: Linh(nữ),Bách(nam),Duy(nữ),Tuấn(nam),Lịch(nữ),Cảnh(nam); CH21: Linh(nữ),Cảnh(nam); CH22: Linh(nữ),Cảnh(nam); CH23: Linh(nữ,stays nữ),Cảnh(nam); CH24: Linh(nữ),Lịch(nữ),Tuấn(nam→now nữ per ch17 Rửa),Bách(nam),Duy(nữ),Cảnh(nam) | Rửa changes character bodies. Prompt accuracy depends on correct gender form per chapter. Based on chapter summaries and MYSTERY.md. | Yes | agent |
| D005 |  | workflow | Manga prompt generation approach for LLM output limits | Incremental trang-by-trang writes: header first, then each trang as separate edit/append step | LLM backend cannot emit 300+ lines in a single write. Each prompt file (~180-310 lines) must be built page-by-page. This keeps each write within token limits (~100-150 lines per step) while producing identical final output. Pattern: write header → append trang 1 → append trang 2 → ... → append final trang + notes. | Yes | human |
| D006 | M002 S03/S04 planning | process | Incremental trang-by-trang writing for manga prompt generation | All manga prompt tasks (S01–S04) must use incremental trang-by-trang writes instead of single-file writes | LLM backend cannot emit 300+ lines in one response without timeout errors. Breaking prompt generation into trang-level chunks (file header first, then each trang appended separately) keeps each write within the ~150 line token budget. S01 and S02 already proved this pattern works. S03 and S04 task plans now updated to match. | Yes | human |
| D007 |  | execution | Large file generation must use incremental writes to avoid LLM timeout | Hard rule in KNOWLEDGE.md: write first section with `write`, append subsequent sections with `cat >> file << 'EOF'`. Never single-shot write files over ~100 lines. Task plan instructions on write patterns are mandatory, not suggestions. | Repeated task failures from LLM output token truncation when writing large files (200-600+ lines) in a single write call. The truncation is silent — produces broken/incomplete files that then need manual cleanup. Task plans explicitly specify incremental writes but the agent ignores them. Making this a top-level hard rule in KNOWLEDGE.md ensures it's injected into every session's system context. | Yes | human |
